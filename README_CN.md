[中文版](README_CN.md) | [English](README.md)

# 贪吃蛇游戏 - 面向对象编程版本

## 📋 简介

这是使用**面向对象编程**实现的贪吃蛇游戏。代码使用类和对象，展示了面向对象编程的优势和特性。

## 🎯 编程风格特点

### 1. 使用类和对象
```python
class Snake:
    def __init__(self, start_pos, initial_length):
        self.body = [start_pos]
        self.direction = Direction.RIGHT
    
    def move(self):
        # 移动逻辑
```

### 2. 封装
- ✅ 数据和操作封装在类中
- ✅ 通过方法访问和修改状态
- ✅ 隐藏内部实现细节

### 3. 职责分离
- `Snake` 类：管理蛇的状态和行为
- `Food` 类：管理食物的生成和位置
- `GameBoard` 类：管理游戏状态
- `GameRenderer` 类：负责绘制
- `SnakeGameGUI` 类：管理GUI界面

## 📁 代码结构

```
oop_version/
├── main.py                 # 主入口文件（直接导入并运行SnakeGameGUI）
└── src/                    # 源代码文件夹
    ├── __init__.py         # 包初始化文件
    ├── direction.py        # Direction 枚举
    ├── point.py            # Point 数据类
    ├── game_config.py      # GameConfig 配置类
    │   ├── 速度配置（INITIAL_GAME_SPEED, MIN_GAME_SPEED等）
    │   └── 颜色配置
    ├── snake.py            # Snake 类 - 管理蛇的状态和行为
    ├── food.py             # Food 类 - 管理苹果（随机生成位置）
    ├── game_board.py       # GameBoard 游戏逻辑类
    │   ├── update() - 更新游戏状态（速度递增逻辑）
    │   ├── current_speed - 动态速度管理
    │   └── reset() - 重置游戏（重置速度）
    ├── game_renderer.py    # GameRenderer 渲染器类
    │   ├── draw_apple() - 绘制苹果emoji
    │   ├── draw_snake_head() - 绘制蛇头（带眼睛）
    │   ├── draw_snake_body() - 绘制蛇身
    │   ├── draw_snake_tail() - 绘制蛇尾
    │   └── draw_snake() - 绘制完整蛇
    └── snake_game_gui.py   # SnakeGameGUI GUI主类
        ├── _create_widgets() - 创建界面（优化按钮样式）
        └── _game_loop() - 使用动态速度
```

**模块化设计优势**：
- ✅ 每个文件职责单一
- ✅ 易于维护和测试
- ✅ 代码复用性强
- ✅ 依赖关系清晰

详细说明请查看 `STRUCTURE.md`

## 🔧 环境配置

### macOS
1. **安装 Python 3.11+**（如果尚未安装）：
   ```bash
   brew install python3
   ```

2. **安装 tkinter**（GUI 所需）：
   ```bash
   brew install python-tk
   ```

3. **验证安装**：
   ```bash
   python3 -c "import tkinter; print('tkinter 已就绪!')"
   ```

### Windows
1. **从 [python.org](https://www.python.org/downloads/) 安装 Python 3.11+**
   - ✅ 安装时勾选 "Add Python to PATH"
   - ✅ Windows 版本的 Python 默认包含 tkinter

2. **验证安装**：
   ```cmd
   python -c "import tkinter; print('tkinter 已就绪!')"
   ```

## 🎮 运行方式

```bash
python3 main.py
```

或者：

```bash
cd snake-game-oop
python3 main.py
```

## ⚙️ 游戏控制

- **方向键** 或 **WASD**：控制蛇的移动
- **空格键**：暂停/继续
- **重新开始按钮**：开始新游戏

## 🎮 游戏特色

### 速度系统
- **初始速度**：600ms（很慢，适合新手）
- **速度递增**：每吃一个苹果🍎，速度增加（延迟减少5ms）
- **最快速度**：50ms（挑战极限）
- **重新开始**：速度重置为初始值

### 图形设计
- **苹果**：使用红色苹果emoji 🍎，清晰醒目
- **蛇头**：绿色圆形，带眼睛（根据移动方向显示）
- **蛇身**：绿色圆形，渐变效果
- **蛇尾**：较小的绿色圆形，区分明显
- **所有蛇的部分**：统一的绿色系配色

### 苹果生成
- **随机位置**：每次生成时使用`random.randint()`随机选择位置
- **智能避让**：确保苹果不会生成在蛇身上
- **即时刷新**：吃到苹果后立即生成新位置

### UI优化
- **按钮样式**：浅灰背景 + 深色文字，清晰易读
- **视觉反馈**：按钮有立体效果（raised relief）
- **界面美观**：统一的配色方案

## 💡 面向对象编程的优势

### ✅ 优点

1. **封装性**
   - 数据和操作封装在一起
   - 隐藏内部实现细节
   - 提供清晰的接口

2. **可维护性**
   - 代码组织清晰
   - 易于理解和修改
   - 职责分离明确

3. **可扩展性**
   - 易于添加新功能
   - 可以继承和扩展类
   - 支持多态

4. **可复用性**
   - 类可以在其他项目中复用
   - 减少代码重复

5. **类型安全**
   - 使用类型提示
   - 枚举类型避免魔法值
   - 数据类提供结构化数据

## 📚 学习要点

### 1. 类的设计
```python
class Snake:
    def __init__(self, start_pos, initial_length):
        # 初始化
    def move(self):
        # 行为
    def check_collision(self):
        # 行为
```

### 2. 枚举类型
```python
class Direction(Enum):
    UP = (0, -1)
    DOWN = (0, 1)
```

### 3. 数据类
```python
@dataclass
class Point:
    x: int
    y: int
```

### 4. 类型提示
```python
def move(self) -> None:
def get_head(self) -> Point:
```

### 5. 职责分离
- 每个类只负责一件事
- 类之间通过接口交互
- 降低耦合度

## 🔄 对比函数式版本

查看 `../functional_version/` 了解两种编程风格的差异！

## 🎓 高级特性

### 1. 运算符重载
```python
def __add__(self, other):
    # 重载 + 运算符
```

### 2. 类方法
```python
@classmethod
def generate_random(cls, ...):
    # 类方法
```

### 3. 属性访问
```python
@property
def position(self):
    return self._position
```

## 💻 代码示例

### 创建蛇对象
```python
snake = Snake(Point(10, 10), initial_length=3)
snake.move()
snake.grow()
```

### 创建游戏板
```python
board = GameBoard(width=20, height=20)
board.update()
state = board.get_state()
```

### 使用渲染器
```python
renderer = GameRenderer(canvas, cell_size=25)
renderer.draw_snake(snake.get_body())
renderer.draw_food(food.get_position())
```

## 🚀 扩展建议

1. **添加新功能**
   - ~~不同难度级别~~ ✅ 已实现：速度随分数递增
   - 障碍物
   - 特殊食物（加速、减速）

2. **改进代码**
   - 添加单元测试
   - 使用设计模式（观察者模式等）
   - 添加配置文件

3. **学习新概念**
   - 继承和多态
   - 抽象基类
   - 设计模式
